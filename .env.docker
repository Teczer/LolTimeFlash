# LolTimeFlash - Docker Production Environment

# Next.js - Enable standalone for Docker
STANDALONE_BUILD=true

# Socket.IO - API URL (localhost from browser perspective)
NEXT_PUBLIC_SOCKET_PORT=http://localhost:8888

# API Configuration
NODE_ENV=production
PORT=8888
LOG_LEVEL=info
