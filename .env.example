# LolTimeFlash - Environment Variables Example
# Copy this file to .env and configure for your environment

# ============================================
# DEVELOPMENT (Local)
# ============================================

# Next.js - Standalone build (Docker only)
# Set to 'true' only for Docker builds, leave empty for local dev
STANDALONE_BUILD=

# Socket.IO - API URL for frontend
NEXT_PUBLIC_SOCKET_PORT=http://localhost:4000

# API - Server configuration
NODE_ENV=development
PORT=4000
LOG_LEVEL=debug

# ============================================
# PRODUCTION (Docker)
# ============================================

# For Docker builds, use .env.docker or set:
# STANDALONE_BUILD=true
# NODE_ENV=production
# LOG_LEVEL=info
